# Domain address
DOMAIN_ADDRESS={{ DOMAIN_ADDR }}
TRA_SUB={{ TRAEFIK_SUB_DOMAIN }}

# ACME variables
ACME_EMAIL=cert@cloudflow.ir

# set restart policy
RESTART_POLICY=on-failure

#  web auth information.
#  how to generate password:
#  echo <TRAEFIK_WEB_PASSWROD> | htpasswd -s -n -i username


WEB_AUTH_USER={{ TRAEFIK_USER }}
WEB_AUTH_PASS={{ TRAEFIK_PASS }}

# Hostname
HOSTNAME=reverse-proxy-server
